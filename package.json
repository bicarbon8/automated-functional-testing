{
    "name": "automated-functional-testing",
    "version": "1.0.0",
    "main": "index.js",
    "author": "Jason Holt Smith  <bicarbon8@gmail.com>",
    "license": "MIT",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "clean": "yarn run rimraf **/dist",
        "build": "yarn clean && yarn tsc -b",
        "test": "yarn workspace aft-core test && yarn workspace aft-logging-awskinesis test && yarn workspace aft-testrail test && yarn workspace aft-ui test && yarn workspace aft-ui-browsers test && yarn workspace aft-ui-mobile-apps test && yarn workspace aft-web-services test && yarn workspace aft-logging-html test"
    },
    "dependencies": {
        "lerna": "^4.0.0"
    }
}
